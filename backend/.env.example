PORT=4020
# Render injects PORT automatically - local dev uses 4020
POSTGRES_HOST=dipartners.co.uk
POSTGRES_PORT=55432
POSTGRES_DB=master_database
POSTGRES_USER=edward
POSTGRES_PASSWORD=your_password_here
# On Render: set these 5 as Environment Variables (Dashboard -> Environment, sync:false)
# They are NOT committed - see render.yaml envVars sync:false
# Optional: separate pools for norgate/finviz (fall back to POSTGRES_* if unset)
# NORGATE_POSTGRES_HOST=dipartners.co.uk
# NORGATE_POSTGRES_PORT=55432
# NORGATE_DB=norgate_candlestick
# NORGATE_POSTGRES_USER=edward
# NORGATE_POSTGRES_PASSWORD=your_password_here
